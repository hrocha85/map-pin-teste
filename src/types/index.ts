export interface UserType {
  email: string;
  password: string;
}

export interface ContactType {
  name: string;
  cpf: string;
  phone: string;
  cep: string;
  address: string;
  city: string;
  state: string;
}
